{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*",
    "docs"
  ],
  "type": "module",
  "scripts": {
    "cs:ver": "changeset version && pnpm install --lockfile-only",
    "cs:pub": "changeset publish",
    "cs:add": "changeset add",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "release": "node scripts/publish.ts"
  },
  "devDependencies": {
    "@changesets/cli": "^3.0.3",
    "@lxsmnsyc/oxlint-config": "^0.1.2",
    "@types/node": "^25.0.3",
    "oxfmt": "^0.62.0",
    "oxlint": "^1.77.0",
    "oxlint-tsgolint": "^7.0.2001",
    "typescript": "^5.9.3"
  }
}
